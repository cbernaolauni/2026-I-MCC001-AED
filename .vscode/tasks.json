{
    "version": "2.0.0",
    "tasks": [
        {
            "type": "shell",
            "label": "Ejecutar Make",
            "command": "mingw32-make",
            "args": [
                "-B"
            ],
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "problemMatcher": [
                "$gcc"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "detail": "Compilación del proyecto usando mingw32-make -B"
        }
    ]
}